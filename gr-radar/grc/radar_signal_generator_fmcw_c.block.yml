# auto-generated by grc.converter

id: radar_signal_generator_fmcw_c
label: Signal Generator FMCW
category: "[RADAR]/Generators"

parameters:
  - id: samp_rate
    label: Sample rate
    dtype: int
  - id: samp_cw
    label: Legacy Samples CW
    dtype: int
    hide: ${ 'all' if int(lora_sf) > 0 and float(lora_bw) > 0 else 'none' }
  - id: samp_up
    label: Legacy Samples up-chirp
    dtype: int
    hide: ${ 'all' if int(lora_sf) > 0 and float(lora_bw) > 0 else 'none' }
  - id: samp_down
    label: Legacy Samples down-chirp
    dtype: int
    hide: ${ 'all' if int(lora_sf) > 0 and float(lora_bw) > 0 else 'none' }
  - id: freq_cw
    label: Legacy Start Frequency
    dtype: float
    hide: ${ 'all' if int(lora_sf) > 0 and float(lora_bw) > 0 else 'none' }
  - id: freq_sweep
    label: Legacy Sweep Span
    dtype: float
    hide: ${ 'all' if int(lora_sf) > 0 and float(lora_bw) > 0 else 'none' }
  - id: amplitude
    label: Amplitude
    dtype: float
  - id: repeat_count
    label: Triangle Count
    dtype: int
    default: 1
  - id: len_key
    label: Packet length key
    dtype: string
    default: '"packet_len"'
  - id: lora_sf
    label: LoRa SF
    dtype: int
    default: 0
  - id: lora_bw
    label: LoRa BW
    dtype: float
    default: 0.0

outputs:
  - label: IQ out
    domain: stream
    dtype: complex

templates:
  imports: from gnuradio import radar
  make: radar.signal_generator_fmcw_c(${samp_rate}, ${samp_up}, ${samp_down}, ${samp_cw}, ${freq_cw}, ${freq_sweep}, ${amplitude}, ${repeat_count}, ${len_key}, ${lora_sf}, ${lora_bw})

file_format: 1
