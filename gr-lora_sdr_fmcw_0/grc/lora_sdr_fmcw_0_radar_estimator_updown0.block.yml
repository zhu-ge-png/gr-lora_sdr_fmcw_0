# auto-generated by grc.converter

id: lora_sdr_fmcw_0_radar_estimator_updown0
label: Radar Estimator UpDown0
category: '[LoRa_RX]'
flags: [python,cpp]

parameters:
-   id: samp_rate
    label: Sample rate
    dtype: int
-   id: center_freq
    label: Center frequency
    dtype: real
-   id: sweep_freq
    label: Sweep frequency
    dtype: real
-   id: samp_up
    label: Samples up-chirp
    dtype: int
-   id: samp_down
    label: Samples down-chirp
    dtype: int
-   id: push_power
    label: Push power
    dtype: enum
    options: ['True', 'False']
    default: 'False'

inputs:
-   domain: message
    id: Msg in UP
    optional: 1
-   domain: message
    id: Msg in DOWN
    optional: 1

outputs:
-   domain: message
    id: Msg out
    optional: 1

templates:
    imports: import gnuradio.lora_sdr_fmcw_0 as lora_sdr
    make: lora_sdr.radar_estimator_updown0(${samp_rate}, ${center_freq}, ${sweep_freq}, ${samp_up}, ${samp_down}, ${push_power})

cpp_templates:
    includes: ['#include <gnuradio/lora_sdr_fmcw_0/radar_estimator_updown0.h>']
    declarations: 'gr::lora_sdr_fmcw_0::radar_estimator_updown0::sptr ${id};'
    make: 'this->${id} = gr::lora_sdr_fmcw_0::radar_estimator_updown0::make(${samp_rate}, ${center_freq}, ${sweep_freq}, ${samp_up}, ${samp_down}, ${push_power});'
    translations:
        'False': 'false'
        'True': 'true'

documentation: |-
    Estimate FMCW range and velocity from up/down beat frequencies only.
        Inputs:
            Msg in UP: message from a peak detector fed by the up-chirp FFT
            Msg in DOWN: message from a peak detector fed by the down-chirp FFT
        Output:
            Msg out: message containing rx_time, range and velocity vectors

file_format: 1
