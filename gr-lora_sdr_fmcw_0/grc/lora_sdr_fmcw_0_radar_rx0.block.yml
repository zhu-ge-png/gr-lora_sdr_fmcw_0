# auto-generated by grc.converter

id: lora_sdr_fmcw_0_radar_rx0
label: Radar Rx0
category: '[LoRa_RX]'
flags: [python,cpp]

parameters:
-   id: bandwidth
    label: Bandwidth
    dtype: int
-   id: sf
    label: SF
    dtype: int
-   id: os_factor
    label: OS factor
    dtype: int
    default: 4
-   id: corr_threshold
    label: Corr threshold
    dtype: real
    default: 0.45
-   id: len_tag_key
    label: Length tag key
    dtype: string
    default: '"radar_len"'
    hide: part
-   id: show_log_port
    dtype: enum
    options: ['True', 'False']
    default: 'False'
    hide: part

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
-   domain: stream
    label: corr_log
    dtype: float
    optional: 1
    hide: ${False if str(show_log_port)=='True' else True}

templates:
    imports: import gnuradio.lora_sdr_fmcw_0 as lora_sdr
    make: lora_sdr.radar_rx0(${bandwidth}, ${sf}, ${os_factor}, ${corr_threshold}, ${len_tag_key})

cpp_templates:
    includes: ['#include <gnuradio/lora_sdr_fmcw_0/radar_rx0.h>']
    declarations: 'gr::lora_sdr_fmcw_0::radar_rx0::sptr ${id};'
    make: 'this->${id} = gr::lora_sdr_fmcw_0::radar_rx0::make(${bandwidth}, ${sf}, ${os_factor}, ${corr_threshold}, ${len_tag_key});'
    translations:
        'False': 'false'
        'True': 'true'

documentation: |-
    Receive LoRa-aligned FMCW radar triangles using normalized correlation thresholding.
        Parameters:
            bandwidth: LoRa bandwidth [Hz]
            sf: spreading factor
            os_factor: oversampling factor of the complex input stream
            corr_threshold: minimum normalized correlation required to accept one FMCW triangle
            len_tag_key: tag key attached at the start of each accepted triangle, with value equal to the triangle length in samples
        Input:
            in: complex-valued input stream
        Output:
            out: accepted FMCW triangles only; each accepted triangle is tagged with len_tag_key and radar_corr
            corr_log: optional float stream carrying the accepted correlation value over each output triangle

file_format: 1
