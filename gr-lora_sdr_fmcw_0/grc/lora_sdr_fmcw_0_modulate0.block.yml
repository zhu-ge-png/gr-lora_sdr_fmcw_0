# auto-generated by grc.converter

id: lora_sdr_fmcw_0_modulate0
label: Modulate0
category: '[LoRa_TX]'

parameters:
-   id: sf
    label: SF
    dtype: int
    default: 'sf'
-   id: samp_rate
    label: Samp_rate
    dtype: int
-   id: bw
    label: BW
    dtype: int
-   id: sync_words
    label: Sync words
    dtype: int_vector
    hide: part
    default: 0x12
-   id: preamb_len
    label: Preamble_len
    dtype: int
    default: 8
    hide: part
-   id: zero_preamble_chirps
    label: Zero-bin preamble chirps
    dtype: int
    default: 0
    hide: part
-   id: frame_zero_padd
    label: Frame zero padding
    hide: part
    dtype: int
    default: int(20*2**sf*samp_rate/bw)

inputs:
-   domain: stream
    dtype: int

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import gnuradio.lora_sdr_fmcw_0 as lora_sdr
    make: lora_sdr.modulate0(${sf}, ${samp_rate}, ${bw}, ${sync_words}, ${frame_zero_padd},${preamb_len}, ${zero_preamble_chirps})
asserts:
- ${ (samp_rate/bw).is_integer()}
- ${frame_zero_padd>=0}
- ${zero_preamble_chirps>=0}
- ${zero_preamble_chirps<=preamb_len}

documentation: |-
    Add the preamble to the frame and modulate lora symbols according to the input decimal values.
        Parameters:
            sf: spreading factor
            samp_rate: sampling rate (need to be a integer multiple of the bandwidth)
            Bw: LoRa bandwidth
            preamb_len: Total number of preamble upchirp units. Should be in [6-65535] (default value 8);
            zero_preamble_chirps: Number of retained 0-bin LoRa upchirps before the sync words.
            Frame zero padding: zero padding append after each frame. Value given in number of samples with a minimum of 1
        Input:
            in: stream of decimal symbol values
        Output:
            out: stream of complex valued samples

file_format: 1
